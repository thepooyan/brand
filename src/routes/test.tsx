import { onMount } from "solid-js"
import FallbackPage from "~/components/pages/FallbackPage"

const test = () => {
  onMount(async() => {
  })
  return (
    <FallbackPage/>
  )
}

export default test
